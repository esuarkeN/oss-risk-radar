# `@oss-risk-radar/config`

Shared configuration package placeholder.

Planned contents:

- shared ESLint presets for frontend workspaces
- Prettier or formatting defaults if introduced later
- generated client configuration once OpenAPI codegen is added
